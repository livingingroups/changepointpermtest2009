% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpt_plots.R
\name{plot.change_point_test}
\alias{plot.change_point_test}
\title{Plot Change point test output}
\usage{
\method{plot}{change_point_test}(
  x,
  direction = FALSE,
  cp_col = "black",
  arrow_length = 0.1,
  arrow_code = 2,
  arrow_col = "black",
  arrow_lty = 3,
  arrow_lwd = 1,
  nfacet_col = NULL,
  ...
)
}
\arguments{
\item{x}{an object of class \code{change_point_test}}

\item{direction}{logical indicator if the path direction should be added to the plot}

\item{cp_col}{color of the change points}

\item{arrow_length}{length of the arrow of the direction (argument passed to 
\code{\link[graphics]{arrows}})}

\item{arrow_code}{code of the arrow of the direction (argument passed to 
\code{\link[graphics]{arrows}})}

\item{arrow_col}{color of the arrow of the direction (argument passed to 
\code{\link[graphics]{arrows}})}

\item{arrow_lty}{line type of the arrow of the direction (argument passed to 
\code{\link[graphics]{arrows}})}

\item{arrow_lwd}{line width of the arrow of the direction (argument passed to 
\code{\link[graphics]{arrows}})}

\item{nfacet_col}{number of columns used in facet.args argument ncol}

\item{...}{other arguments used in \code{\link[tinyplot]{tinyplot}}}
}
\description{
Plots change points of objects of class \code{\link[cpt]{change_point_test}}
based on \code{\link[tinyplot]{tinyplot}} functionality.
}
\examples{
library(cpt)
library(trackframe)
library(tinyplot)

data("cptfiguredata_tf")
data <- cptfiguredata_tf[startsWith(cptfiguredata_tf$track_id, '4'),]
class(data)
tinytheme("clean2")
plot(data)
# single track
plot(select_id(data, "4a"))

# calculate change points
cpt <- change_point_test(data, alpha = .01, N = 10000, q = 6)
class(cpt)

plot(cpt)
# with path directions
plot(cpt, direction = TRUE)

# only one track
cpt4a <- select_id(cpt, "4a")
plot(cpt4a)
# with path direction
plot(cpt4a, direction = TRUE)
}
